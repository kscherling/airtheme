import { FONT_WEIGHT } from '@airtheme/core'
import { STRING } from '@airtheme/core'

export default {
  view: STRING,
  viewable: [STRING],
  reference: null,
  object: FONT_WEIGHT,
  unit: STRING
}
