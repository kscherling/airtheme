import { BASIC, VERSION } from '@airtheme/core'

// base
// The container object

export default {
  name: 'Basic Airtheme',
  type: BASIC,
  baseFontSize: 16,
  baseLineHeight: 16,
  baseSpacing: 16,
  version: VERSION
}
