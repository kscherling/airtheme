import { BASIC } from '../../constants/themeThemes'
import { VERSION } from '../../constants/versions'

// theme
// The container object

export default {
  name: 'Basic Airtheme',
  type: BASIC,
  baseFontSize: 16,
  baseLineHeight: 1.15,
  baseSpacing: 16,
  version: VERSION
}
