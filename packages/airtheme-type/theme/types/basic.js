import { THEME } from '../../constants/themeTheme'
import { VERSION } from '../../constants/version'

// theme
// The container object

export default {
  name: 'Basic Airtheme',
  baseFontSize: 16,
  baseLineHeight: 1.15,
  baseSpacing: 16,
  version: VERSION
}
