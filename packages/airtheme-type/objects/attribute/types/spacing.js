import { SPACING } from '../../../constants/attributeTypes'
import { FACTOR, PX } from '../../../constants/unitTypes'
import { BASE_SPACING } from '../../../constants/themeKeys'

export default {
  display: FACTOR,
  displays: [FACTOR, PX],
  reference: BASE_SPACING,
  type: SPACING,
  unit: FACTOR
}
