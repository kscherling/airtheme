import { FONT_WEIGHT } from '../../../constants/themeAttributes'
import { STRING } from '../../../constants/themeUnits'

export default {
  display: STRING,
  displays: [STRING],
  reference: null,
  type: FONT_WEIGHT,
  unit: STRING
}
