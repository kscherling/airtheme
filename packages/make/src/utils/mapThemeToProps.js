// NOTE: This should likely be an aspect of core or make or type?

export const mapThemeSwatch = ({ swatch }) => ({ swatch })
export const mapThemeBaseFontSize = ({ baseFontSize }) => ({ baseFontSize })
export const mapThemeBaseLineHeight = ({ baseLineHeight }) => ({ baseLineHeight }) // prettier-ignore
export const mapThemeFontWeight = ({ setting: { fontWeight } }) => ({ fontWeight }) // prettier-ignore
