{
  "name": "airtheme",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "lerna run build",
    "clean": "lerna run clean",
    "test": "lerna run test",
    "check-packages": "yarn clean && yarn build && yarn test"
  }
}
